\usepackage{amsmath}

\usepackage{ifluatex}
% Packages Génériques
\ifluatex
    % Lua(La)TeX
    \usepackage[french]{babel}
    \usepackage{fontspec}
    \defaultfontfeatures{Ligatures=TeX}
    \usepackage{unicode-math} 
    \unimathsetup{math-style=TeX}
\else
    %default: pdf(La)TeX
    \usepackage[french]{babel}
    \usepackage{lmodern} % load vector font
    \usepackage[T1]{fontenc} % font encoding
    \usepackage[utf8]{inputenc} % input encoding
\fi

% Définition des couleurs personalisées
\usepackage{xcolor}
\input{Meta/colors.tex}

% Tikz
\usepackage{tikz}
\input{Meta/tikzset.tex}

% Flottants
\usepackage[above]{placeins}
\usepackage[lofdepth,lotdepth]{subfig}

% Paratexte
\usepackage[babel]{csquotes}

% Toc locales
\usepackage{etoc}

% Remplacement de include et input
\usepackage{import}

% Affichage de chiffres
\input{Meta/config_siunitx.tex}

% Représentation des dialogues
\usepackage{dialogue}

% tableaux
% Gestion avancée tableaux
\usepackage{array}

% Modification de la géométrie (page de garde)
\usepackage{geometry}

% Bibliographie
\input{Meta/config_bibliographie.tex}

% Hyperef à la fin, sauf cas particuliers
\usepackage{hyperref}
% Configuration du package
\hypersetup{
    % non-Latin characters in Acrobat’s bookmarks         
    unicode=true,  
    % Métadonnées    
    pdftitle={Téàð@€đ«đŋ},
    pdfauthor={Mattia Bunel},
    pdfsubject={Sujet},
    pdfkeywords={keyword1, key2, key3},
    % Liens
    % Type
    colorlinks=true,
    % Type Citation 
    linktoc=page,
    % Couleurs
    % ref internes
    linkcolor=RdBu-9-2,
    % citations
    citecolor=RdBu-9-8,
    % fichiers externes
    filecolor=RdBu-9-2,
    % Url
    urlcolor=RdBu-9-8
}

% Glossaire
\usepackage[acronym, subentrycounter, toc]{glossaries}
%% Fichier glossaire
\loadglsentries{Glossaire/glossaire_def}


% Debug
\usepackage{showlabels}
\usepackage{lipsum}
%\usepackage{showframe} 


% Commandes make
\makeglossaries
