\begin{tikzpicture}
  \matrix [matrix of nodes,
  anchor=west,
  nodes={minimum size=.75cm}
  ] (zla0) at (0,0)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };

  \matrix [matrix of nodes,
  anchor=west,
  nodes={minimum size=.75cm},
  right=.5cm of zla0
  ] (zla1)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);&
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };

  \matrix [matrix of nodes,
  anchor=west,
  nodes={minimum size=.75cm},
  right=.5cm of zla1
  ] (zla2)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);&
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };
  \matrix [matrix of nodes,
  anchor=west,
  nodes={minimum size=.75cm},
  right=.5cm of zla2
  ] (zla3)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);&
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };
  \matrix [matrix of nodes,
  anchor=west,
  nodes={minimum size=.75cm},
  right=.5cm of zla3
  ] (zla4)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);&
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };
  \matrix [matrix of nodes,
  anchor=west,
  nodes={minimum size=.75cm},
  right=.5cm of zla4
  ] (zla5)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);&
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };


  \matrix [matrix of nodes,
  anchor=north,
  nodes={minimum size=.75cm},
  below=1.5cm of zla0
  ] (zlb0)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };

  \matrix [matrix of nodes,
  anchor=north,
  nodes={minimum size=.75cm},
  below=1.5cm of zla1
  ] (zlb1)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };

  \matrix [matrix of nodes,
  anchor=north,
  nodes={minimum size=.75cm},
  below=1.5cm of zla2
  ] (zlb2)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };
  \matrix [matrix of nodes,
  anchor=north,
  nodes={minimum size=.75cm},
  below=1.5cm of zla3
  ] (zlb3)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };
  \matrix [matrix of nodes,
  anchor=north,
  nodes={minimum size=.75cm},
  below=1.5cm of zla4
  ] (zlb4)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };
  \matrix [matrix of nodes,
  anchor=north,
  nodes={minimum size=.75cm},
  below=1.5cm of zla5
  ] (zlb5)
  {
    \draw[ffa,ffc] (0,0) circle (.3cm);\\
  };


  % Accolades
  \foreach \m in {0,1,...,5} {
    \draw (zla\m.south west) |- ($(zla\m.south west)!0.5!(zla\m.south
    east) + (0,-.1)$) -| (zla\m.south east)  node[pos=0, yshift=.2]
    (zla\m-g) {};
    % 
    \draw (zlb\m.north west) |- ($(zlb\m.north west)!0.5!(zlb\m.north
    east) + (0,.1)$) -| (zlb\m.north east)  node[pos=0, yshift=.2]
    (zlb\m-g) {};
    % 
    \draw[->>, black,] ([yshift=-.1cm]zla\m.south) -- ([yshift=.1cm]zlb\m.north);
  }
\end{tikzpicture}